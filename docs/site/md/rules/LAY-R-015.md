# LAY-R-015 — Rules for the ASG Edge+ rule engine

*Configuration, Layouts, Forms & Reporting · Derived*

**Candidate driver fields are: the layout's primary table, plus every table reachable by a many-to-one FK from it, plus `ProjectEntity`.**

Candidate driver fields are: the layout's primary table, plus every table reachable by a many-to-one FK from it, plus `ProjectEntity`.

## What it constrains

[ProjectEntity](../entities/ProjectEntity.md)

---

Source: `docs/modules/layouts-and-forms/conditional-fields.md`
