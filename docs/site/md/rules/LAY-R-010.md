# LAY-R-010 — Rules for the ASG Edge+ rule engine

*Configuration, Layouts, Forms & Reporting · Derived*

**A conditional rule set attaches to exactly one target, identified by a stable key. A target is either a single field or an entire sub-page section.**

A conditional rule set attaches to exactly one target, identified by a stable key. A target is either a single field or an entire sub-page section.

---

Source: `docs/modules/layouts-and-forms/conditional-fields.md`
