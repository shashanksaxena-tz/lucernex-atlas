# LAY-R-012 — Rules for the ASG Edge+ rule engine

*Configuration, Layouts, Forms & Reporting · Derived*

**A rule set declares exactly one quantifier over its predicates: `ALL` or `ANY`. Nesting is not supported.**

A rule set declares exactly one quantifier over its predicates: `ALL` or `ANY`. Nesting is not supported.

---

Source: `docs/modules/layouts-and-forms/conditional-fields.md`
