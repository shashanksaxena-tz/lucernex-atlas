# ASC 842 Accounting

Lease accounting and compliance. One engine serves three standards - ASC 842, IFRS 16 and legacy straight-line. There is no separate ASC 842 module: a Summary/Period record pair (SLSummary, 134 fields; SLPeriod, 79) carries the schedule, and three mutually exclusive flags say which standard a schedule was produced under.

## Who it is for

*Derived · fact · source: `docs/features/README.md`*

Lease accountants and the controller's office. They do not configure anything: they press Generate Rent or Calculate Schedule on a contract, review what comes out, and route it for approval. The output is what the auditors see.

## Where it is used

*Observed · fact · source: `docs/data-model/screen-routing.md`*

The contract record's accounting tabs, the schedule review screens, and the approval steps of the accounting workflow. The engine has no screen of its own — it is a button on a record.

## Classification record

*Observed · capability · source: `docs/modules/accounting/asc-842.md + View Object Model`*

A separate 93-field record (ContractFinancialTest) runs the five ASC 842 classification tests and holds ASC 842 and IFRS 16 results side by side. An older ASC 840 Cap Lease Test still sits directly on the lease. One field is literally the 90%-test as a formula: initial liability balance over threshold fair value - the 'substantially all of the fair value' criterion, computed.

## Three term lengths

*Observed · capability · source: `docs/modules/contracts/cam-waterfall.md`*

Classification uses three different term lengths - contractual, test and 'likely'. The term used for classification is not necessarily the contractual term, because renewal options the lessee is reasonably certain to exercise extend it. A rebuild needs all three, not one.

## User-triggered runs

*Observed · capability · source: `docs/screens/014-contract-record-end-user.md`*

Schedules are generated by a person pressing a button on the record - Generate Rent, Calculate Schedule - not by a batch job. The accounting engine is user-triggered. What Generate Rent produces was learned by reading the 11,426 transactions this tenant already has, without writing to the shared training tenant.

## Approved, not published

*Observed · capability · source: `docs/modules/workflow/README.md`*

Schedule output passes through an approval workflow before anyone relies on it: generate, initial review, ASG approve, client approve - three steps. A rebuild needs a schedule state machine, not just a calculator.

## Layered discount rate

*Derived · capability · source: `docs/modules/accounting/rules.md`*

Any calculation needing a discount rate resolves in layers: contract override first, then rate tables scoped by portfolio / country / state, then the portfolio default. The layered fallback is stated as rule ACC-R-001.

## Amortisation switch

*Observed · capability · source: `Live code-table capture`*

Amortisation has exactly one policy switch with two values: per day or per period (GaapAmortizeMode). Day-based versus period-based expense recognition is a single tenant-level choice.

## Equipment leases too

*Observed · capability · source: `docs/modules/assets-equipment/equipment-leases.md`*

The engine runs on equipment, not just real estate: the classification, summary and period records each carry a nullable foreign key to Asset. Embedded equipment leases are first-class, not a workaround.

## Runs ASC 842 only

*Observed · fact*

This tenant runs ASC 842 only: the ASC 842 schedule-type table holds exactly one value, '842 Rent', and the straight-line and IFRS 16 type tables are both empty. The capability is present and unused - whether the rebuild needs IFRS 16 at all is a business question, still open.

## No numeric typing

*Observed · fact*

In the physical database export, 6,882 of 7,069 non-key columns are TEXT - currency and percentages included. There is no numeric typing to inherit. The rebuild's constitution already mandates BigDecimal; Lx shows what happens without it.

## Magnitude-typed field

*Observed · fact · source: `docs/modules/accounting/computed-vs-input-fields.md`*

One accounting field changes meaning by magnitude: it is read as a percentage from 0-100 and as currency at 100.01 and above. A genuine data-integrity hazard to design out, not inherit.

## Evidence

*Observed · fact · source: `docs/assets/screenshots/`*

1 screen captures on disk, under docs/assets/screenshots/accounting — the screens themselves, not a description of them. First few: asc842-schedule-type-code-values.jpg.

![asc842-schedule-type-code-values.jpg](../../assets/screenshots/accounting/asc842-schedule-type-code-values.jpg)

## Open questions (80)

*Inferred · group*

80 things nobody has confirmed for this feature. Each one is work somebody has to do before the feature can be rebuilt with confidence; they are carried here rather than resolved by guessing. Click one for the question and the document that raised it.

### How does an equipment

*Inferred · question · source: `docs/modules/accounting/asc-842.md`*

(new) ⚠ How does an equipment-lease ASC 842 schedule get approved? The review workflow's form type excludes Equipment Contract (ACC-R-061), yet equipment leases generate schedules. Either they bypass the gate or they are approved at portfolio level. Nobody has confirmed this. Recorded in modules/accounting/asc-842.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### What does each of the

*Inferred · question · source: `docs/modules/accounting/asc-842.md`*

(new) What does each of the four ASR form layouts collect? They define the approval record ASG Edge+ must reproduce, and none has been opened. Nobody has confirmed this. Recorded in modules/accounting/asc-842.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### What happens on

*Inferred · question · source: `docs/modules/accounting/asc-842.md`*

(new) What happens on rejection at step 2 or 3? No rollback or re-open path is documented and SLSummary has no "rejected" representation. Nobody has confirmed this. Recorded in modules/accounting/asc-842.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### What are the values of

*Inferred · question · source: `docs/modules/accounting/asc-842.md`*

What are the values of the Schedule Creation Reason Code table? It is the only field that records *why* a schedule exists (initial, remeasurement, modification, reclassification, impairment, term shortening…). Without the enumeration, the schedule-version chain cannot be replayed. Check Admin > Manage Firm Drop Downs. Nobody has confirmed this. Recorded in modules/accounting/asc-842.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Does ASC 842 Test 4

*Inferred · question · source: `docs/modules/accounting/asc-842.md`*

Does ASC 842 Test 4 compare the adjusted or unadjusted initial liability? The vendor text says *"Initial Liability Balance (Adjusted)"* on ThresholdFairValueControlled but names the field plainly as InitialLiabilityBalance on Test4Result. Open an ASC 842 Test on a contract with a non-zero InitialLiabilityBalanceAdjust and read the on-screen comparison. Nobody has confirmed this. Recorded in modules/accounting/asc-842.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Do Test Begin Date

*Inferred · question · source: `docs/modules/accounting/asc-842.md`*

**Do "Test Begin Date" / "Test End Date" exist in the UI as distinct fields from Topic842BeginDate / Topic842EndDate?** TestTermLength's definition names them; the schema does not contain them. Nobody has confirmed this. Recorded in modules/accounting/asc-842.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### What distinguishes

*Inferred · question · source: `docs/modules/accounting/asc-842.md`*

What distinguishes Reclassification* from Reclass* in the roll-forward block? Six fields, two triples, no definitions. Nobody has confirmed this. Recorded in modules/accounting/asc-842.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Which impairment input

*Inferred · question · source: `docs/modules/accounting/asc-842.md`*

Which impairment input wins when SLSummary.ImpairmentAmount, ContractFinancialTest.ImpairmentsAmount and Asset.ImpairmentOverride are all populated?. Nobody has confirmed this. Recorded in modules/accounting/asc-842.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Which numbered

*Inferred · question · source: `docs/modules/accounting/asc-842.md`*

Which numbered ExportAcctNNumber slot is which GL concept? Twenty unnamed slots, copied onto every period row. Now scoped to one row: open 842 Rent at FirmCodeEdit.jsp?TableType=2162 and read the twenty values. The cheapest remaining high-value capture in the module. ASG amortises the ROU asset on every schedule, or the flag is vestigial. Its behaviour cannot be observed in this tenant either way, which leaves ACC-R-030 permanently Inferred. Nobody has confirmed this. Recorded in modules/accounting/asc-842.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### What does Covenant

*Inferred · question · source: `docs/modules/accounting/asc-842.md`*

What does Covenant.HoldAmountInSchedLiability do? No definition; it is the only Boolean on Covenant that names the schedule liability. Nobody has confirmed this. Recorded in modules/accounting/asc-842.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Is Contract

*Inferred · question · source: `docs/modules/accounting/asc-842.md`*

Is Contract.FinalResult (Cap Lease Test group) actually the ASC 842 result? The label and the subgroup disagree. Nobody has confirmed this. Recorded in modules/accounting/asc-842.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### What is the sign

*Inferred · question · source: `docs/modules/accounting/asc-842.md`*

What is the sign convention on PVOfFinancialTermsWithAdjustments? The vendor text says *minus* the adjustments; the per-standard fields say *with*. Nobody has confirmed this. Recorded in modules/accounting/asc-842.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Can a contract carry

*Inferred · question · source: `docs/modules/accounting/asc-842.md`*

Can a contract carry more than one active SLSummary per standard? IsSLSchedule, IsASC842Schedule and IsIFRS16Schedule are three independent Booleans on the same row — nothing in the schema forbids two of them being true at once, nor two active rows of the same kind. Nobody has confirmed this. Recorded in modules/accounting/asc-842.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### What is the initial

*Inferred · question · source: `docs/modules/accounting/asg-edgeplus-mapping.md`*

What is the initial-asset-balance formula? (UI) Open a schedule with known IDC, incentive and prepaid values and back it out. Blocks the measurement service. Nobody has confirmed this. Recorded in modules/accounting/asg-edgeplus-mapping.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Does Money become

*Inferred · question · source: `docs/modules/accounting/asg-edgeplus-mapping.md`*

Does Money become scale-4, or does the accounting service get its own calculation type? Blocks schedule generation. Platform decision, not a Lx question. Nobody has confirmed this. Recorded in modules/accounting/asg-edgeplus-mapping.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### How does the ASG Edge

*Inferred · question · source: `docs/modules/accounting/asg-edgeplus-mapping.md`*

How does the ASG Edge+ reversal model express a lease remeasurement? Lx creates a successor SLSummary with six delta fields and inactivates the predecessor; the BRD's model is reverse-and-repost. These are reconcilable, but the reconciliation has not been written down. Nobody has confirmed this. Recorded in modules/accounting/asg-edgeplus-mapping.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Schedule Creation

*Inferred · question · source: `docs/modules/accounting/asg-edgeplus-mapping.md`*

Schedule Creation Reason Code values. (UI) Admin > Manage Firm Drop Downs. Nobody has confirmed this. Recorded in modules/accounting/asg-edgeplus-mapping.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Which

*Inferred · question · source: `docs/modules/accounting/asg-edgeplus-mapping.md`*

Which ExportAcctNNumber slot is which GL concept in the jcrew tenant? (UI) Needed to map legacy exports onto the BRD's named debit/credit mapping rules. Nobody has confirmed this. Recorded in modules/accounting/asg-edgeplus-mapping.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Is

*Inferred · question · source: `docs/modules/accounting/asg-edgeplus-mapping.md`*

Is SLRemainingAssetBalance's Percent/Currency option-button state persisted anywhere? (UI) If not, the migration of that field is lossy and needs a manual review pass. Nobody has confirmed this. Recorded in modules/accounting/asg-edgeplus-mapping.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Where do Admin Manage

*Inferred · question · source: `docs/modules/accounting/asg-edgeplus-mapping.md`*

Where do Admin > Manage Company > Financial Settings live? (UI) Both the Short-Term/Long-Term Liability Calculation Method and the Translation/Revaluation mapping are there, and neither has a home in the object model. Nobody has confirmed this. Recorded in modules/accounting/asg-edgeplus-mapping.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Does BRD 16 already

*Inferred · question · source: `docs/modules/accounting/asg-edgeplus-mapping.md`*

Does BRD 16 already answer any of the ASC 842 open questions in asc-842.md? Its 1,333 lines were sampled, not read end to end, for this document. A full read against rules.md is the cheapest next step and needs no browser. BRD 16's FR-019 already names an "Approved?" checkbox and a "Recalc?" indicator at schedule level, and FR-028/030/031 already specify recalc-flag reportability, audit drilldown and clear-with-rationale — so some of this is specified. Whether it specifies the *three-step, two-party* gate is the question. Initial Review, Approve (ASG), Approve (Client) — define the approval re. Nobody has confirmed this. Recorded in modules/accounting/asg-edgeplus-mapping.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Is the Cap Lease Test

*Inferred · question · source: `docs/modules/accounting/asg-edgeplus-mapping.md`*

Is the Cap Lease Test read by any live ASG report? (UI / ASG). Nobody has confirmed this. Recorded in modules/accounting/asg-edgeplus-mapping.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Does ASG report under

*Inferred · question · source: `docs/modules/accounting/asg-edgeplus-mapping.md`*

Does ASG report under IFRS 16 at all? If not, items 3 and 4 in "what should deliberately differ" drop to low priority and the IFRS 16 surface migrates as dormant schema. Nobody has confirmed this. Recorded in modules/accounting/asg-edgeplus-mapping.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Does the GraphQL

*Inferred · question · source: `docs/modules/accounting/computed-vs-input-fields.md`*

(new) Does the GraphQL schema expose the canonical FieldType per field? If so, one introspection query verifies all 666 rows against the vendor's own classification. Highest-value follow-up for this file by a wide margin. Nobody has confirmed this. Recorded in modules/accounting/computed-vs-input-fields.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Are the 25

*Inferred · question · source: `docs/modules/accounting/computed-vs-input-fields.md`*

**Are the 25 undocumented roll-forward fields on SLSummary genuinely computed, or are any of them manual overrides? They are the largest block classified COMPUTED on Derived** evidence alone. Open a contract's Roll Forward Report and check whether any cell is editable. Nobody has confirmed this. Recorded in modules/accounting/computed-vs-input-fields.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Which of the twenty

*Inferred · question · source: `docs/modules/accounting/computed-vs-input-fields.md`*

Which of the twenty ExportAcctNNumber slots corresponds to which GL concept? Open a configured ASC 842 Schedule Type record in Admin > Manage Firm Drop Downs and read the labels. Nobody has confirmed this. Recorded in modules/accounting/computed-vs-input-fields.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Is sTYPE PERCENT OR

*Inferred · question · source: `docs/modules/accounting/computed-vs-input-fields.md`*

Is sTYPE_PERCENT_OR_AMOUNT stored with a companion unit flag anywhere? The vendor text describes an option button in the UI; if that selection is persisted, the column holding it is not in the object dump. This determines whether the migration is safe or lossy. Nobody has confirmed this. Recorded in modules/accounting/computed-vs-input-fields.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### What delimiter do the

*Inferred · question · source: `docs/modules/accounting/computed-vs-input-fields.md`*

What delimiter do the *IDList / AssociatedExpenseSetupIDs text columns use?. Nobody has confirmed this. Recorded in modules/accounting/computed-vs-input-fields.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Do ExpenseAccrualSetup

*Inferred · question · source: `docs/modules/accounting/computed-vs-input-fields.md`*

Do ExpenseAccrualSetup.HoldFlag / AccrualTransaction.HoldFlag really have no effect? Both are documented as informational-only, but HoldFlag on ExpenseSchedule and PaymentTransaction is set programmatically by AlternateRentSchedule.SetExpHoldFlag / .SetPRHoldFlag, which implies *some* hold flag is functional. Nobody has confirmed this. Recorded in modules/accounting/computed-vs-input-fields.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Is

*Inferred · question · source: `docs/modules/accounting/computed-vs-input-fields.md`*

Is ContractFinancialTest.YearBuilt really non-functional? It is Fn = No on ContractFinancialTest but is documented as a Test 3 input on Contract. Nobody has confirmed this. Recorded in modules/accounting/computed-vs-input-fields.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Does ASG report under

*Inferred · question · source: `docs/modules/accounting/ifrs-16.md`*

⚠ Does ASG report under IFRS 16 at all — and does it intend to? With the schedule-type table empty and no IFRS 16 workflow, the incumbent is not doing it today. **This is a business question and it decides whether any of this page becomes code.** Everything below is contingent on it. Nobody has confirmed this. Recorded in modules/accounting/ifrs-16.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### resolved 2026 09 10 Is

*Inferred · question · source: `docs/modules/accounting/ifrs-16.md`*

(resolved 2026-09-10) *Is IFRS 16 configured?* — No. TableType 2163 is empty. Nobody has confirmed this. Recorded in modules/accounting/ifrs-16.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### resolved 2026 09 10 Is

*Inferred · question · source: `docs/modules/accounting/ifrs-16.md`*

(resolved 2026-09-10) *Is there an IFRS 16 review/approval process?* — No. Four live workflows, one ASC-842-named, no IFRS-16 counterpart. Nobody has confirmed this. Recorded in modules/accounting/ifrs-16.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Are the IFRS 16

*Inferred · question · source: `docs/modules/accounting/ifrs-16.md`*

Are the IFRS 16 measurement columns nonetheless populated? They are computed on the test record independently of schedule configuration, so they may hold data even with no schedule type configured. Query contract_financial_test for non-null IFRS16InitialLiabilityBalance. **The sharpest remaining technical question on this page** — it separates "IFRS 16 is dead schema" from "IFRS 16 measurement runs silently and nobody looks at it". Nobody has confirmed this. Recorded in modules/accounting/ifrs-16.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### What governs IFRS 16

*Inferred · question · source: `docs/modules/accounting/ifrs-16.md`*

What governs IFRS 16 asset amortisation? Program.SLAssetAmortizeMethod says it affects *"only ASC 842 Finance leases"*, and there is no IFRS 16 counterpart column. Nobody has confirmed this. Recorded in modules/accounting/ifrs-16.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### blocked Does the IFRS

*Inferred · question · source: `docs/modules/accounting/ifrs-16.md`*

(blocked) Does the IFRS 16 schedule use Topic842BeginDate / Topic842EndDate? Cannot be tested in this tenant — with no IFRS 16 schedule type configured, no IFRS 16 schedule can be generated. Needs a tenant where IFRS 16 is set up, or a vendor answer. Nobody has confirmed this. Recorded in modules/accounting/ifrs-16.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Do Asset IsShortTerm

*Inferred · question · source: `docs/modules/accounting/ifrs-16.md`*

**Do Asset.IsShortTerm / Asset.IsLowAssetValue (three-state) override Contract.IsShortTerm / Contract.IsLowAssetValue (two-state), or the reverse?** The three-state type strongly suggests "unset ⇒ inherit from contract", but nothing documents it. Nobody has confirmed this. Recorded in modules/accounting/ifrs-16.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### blocked What actually

*Inferred · question · source: `docs/modules/accounting/ifrs-16.md`*

**(blocked) What actually differs between an ASC 842 schedule and an IFRS 16 schedule at run time?** Given identical code tables and an identical period row, the only observable difference the schema permits is the accounting method and the GL account slots. Not testable here for the same reason as question 6. Nobody has confirmed this. Recorded in modules/accounting/ifrs-16.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### blocked Is

*Inferred · question · source: `docs/modules/accounting/ifrs-16.md`*

(blocked) Is SingleLeaseExpenseAdjustment used on IFRS 16 schedules? If it were, that would be a bug — IFRS 16 lessees have no single-lease-expense model. No IFRS 16 schedule exists to check. Nobody has confirmed this. Recorded in modules/accounting/ifrs-16.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Can IsASC842Schedule

*Inferred · question · source: `docs/modules/accounting/ifrs-16.md`*

Can IsASC842Schedule and IsIFRS16Schedule both be true on one SLSummary? Nothing in the schema prevents it. In this tenant both should be false for IFRS 16 on every row — a true value would indicate a schedule that outlived deletion of its schedule type, which is worth querying for as a data-integrity check regardless. Nobody has confirmed this. Recorded in modules/accounting/ifrs-16.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### How are equipment

*Inferred · question · source: `docs/modules/accounting/rules.md`*

(new) ⚠ How are equipment-lease ASC 842 schedules approved? The review workflow's form type has Equipment Contract = No (ACC-R-061), yet equipment leases produce schedules. Either they skip the gate or they are approved at portfolio level. Blocks the approval-state design. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 033 what is the

*Inferred · question · source: `docs/modules/accounting/rules.md`*

ACC-R-033 — what is the initial-asset-balance formula? The single most important undocumented computation in the module. Every component field exists; the arithmetic does not. Open a schedule in the UI with known IDC/incentive/prepaid values and back it out. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 009 adjusted or

*Inferred · question · source: `docs/modules/accounting/rules.md`*

ACC-R-009 — adjusted or unadjusted initial liability in Test 4? Open an ASC 842 Test with a non-zero InitialLiabilityBalanceAdjust. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### What are the Schedule

*Inferred · question · source: `docs/modules/accounting/rules.md`*

What are the Schedule Creation Reason Code values? Determines whether ACC-R-045 fires on every recalculation or only on modifications. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 024 does

*Inferred · question · source: `docs/modules/accounting/rules.md`*

ACC-R-024 — does creating a RecalcOverrideNotes clear NeedsRecalculation?. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 038 is period

*Inferred · question · source: `docs/modules/accounting/rules.md`*

ACC-R-038 — is period interest computed on the opening or closing liability balance? A one-period difference that compounds across the whole schedule. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 032 what

*Inferred · question · source: `docs/modules/accounting/rules.md`*

ACC-R-032 — what compounding convention discounts a period payment? Asset.CodeCompoundingFrequencyID exists and is undocumented; is there a contract-level equivalent?. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 043 is the

*Inferred · question · source: `docs/modules/accounting/rules.md`*

ACC-R-043 — is the published LiabilityTranslationAdjustment formula wrong?. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 042 what is the

*Inferred · question · source: `docs/modules/accounting/rules.md`*

ACC-R-042 — what is the PV-based short-term-liability formula? Read Admin > Manage Company > Financial Settings. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 030 what does

*Inferred · question · source: `docs/modules/accounting/rules.md`*

ACC-R-030 — what does DontAmortizeAssetValue actually suppress? No vendor definition. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 048 which of the

*Inferred · question · source: `docs/modules/accounting/rules.md`*

ACC-R-048 — which of the three impairment inputs wins?. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 012 does

*Inferred · question · source: `docs/modules/accounting/rules.md`*

**ACC-R-012 — does CodeAcctMethodOverrideID beat the computed FinalResult, and does Asset.CodeAccountingMethodOverrideID beat both?**. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 041 are

*Inferred · question · source: `docs/modules/accounting/rules.md`*

ACC-R-041 — are SLSummary.Forward12Month* and SLPeriod.Forward12Month* the same quantity? The two definitions differ ("change in balance" vs "sum of amortization"). Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 026 where does

*Inferred · question · source: `docs/modules/accounting/rules.md`*

ACC-R-026 — where does the non-secondary remainder of an allocation go?. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 037 sign

*Inferred · question · source: `docs/modules/accounting/rules.md`*

ACC-R-037 — sign convention on PeriodDeferredAmount. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### Do IsShortTerm

*Inferred · question · source: `docs/modules/accounting/rules.md`*

Do IsShortTerm / IsLowAssetValue gate anything at all? If they do not, ASG Edge+ must build the practical-expedient logic from scratch. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 058 what does

*Inferred · question · source: `docs/modules/accounting/rules.md`*

(new) ACC-R-058 — what does Program.SLMatchYearEnds actually match? The only candidate is reconciling Contract's parallel *FiscalYear* and *CalendarYear* rollup blocks. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 059 what are the

*Inferred · question · source: `docs/modules/accounting/rules.md`*

(new) ACC-R-059 — what are the Exchange Rate Type Code values? Fourteen Program columns select from it and none of the values is available offline. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 060 what does an

*Inferred · question · source: `docs/modules/accounting/rules.md`*

(new) ACC-R-060 — what does an ASR form layout collect at each step? Four layouts exist (Submit, Initial Review, Approve (ASG), Approve (Client)). Their field sets define the approval record ASG Edge+ must reproduce, and none has been opened. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

### ACC R 060 what happens

*Inferred · question · source: `docs/modules/accounting/rules.md`*

(new) ACC-R-060 — what happens on rejection at step 2 or 3? Does the schedule return to draft, stay generated-but-unapproved, or get deleted? No rollback path is documented. Nobody has confirmed this. Recorded in modules/accounting/rules.md, under the Lease Accounting & Payments area. Until it is settled, anything built on the assumption is a guess.

## Rules (62)

*Derived · group*

Every numbered rule the docs corpus records for this feature, named by a short summary. Click one: the panel opens with its ID, the full statement, and a link to the complete rule page.

### Default discount — [ACC-R-001](../rules/ACC-R-001.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**resolve Portfolio-level rate first; if none exists, Firm-level rate. Do not read the contract-level rate.**

|  |  |
|---|---|
| When it fires | any calculation that needs a discount rate and finds no contract-level override |
| What it reads | `Program.SLDiscountRate` (`sTYPE_PERCENTAGE`, the portfolio-level default); `DiscountRate` rows (scoped by `ProgramID`, `CountryID`/`CountryIDList`/`StateProvinceIDList`, `CodeContractUseID`, `CodeAccountingMethodID`, `MinSchedMons`/`MaxSchedMons`, `EffectiveThroughDate`); `Contract.ProgramID` |
| What it computes | resolve Portfolio-level rate first; if none exists, Firm-level rate. Do not read the contract-level rate |
| What it writes | `Contract.ComputedSLDiscountRate` (`sTYPE_PERCENTAGE`) |

### Contract level — [ACC-R-002](../rules/ACC-R-002.md)

*Inferred · rule · source: `docs/modules/accounting/rules.md`*

**if `Contract.DiscountRate` is populated it is the rate used; otherwise `ComputedSLDiscountRate`.**

|  |  |
|---|---|
| When it fires | schedule generation or classification test |
| What it reads | `Contract.DiscountRate` (INPUT), `Contract.ComputedSLDiscountRate` (COMPUTED) |
| The test | if `Contract.DiscountRate` is populated it is the rate used; otherwise `ComputedSLDiscountRate` |
| What it writes | `SLSummary.DiscountRate`, `ContractFinancialTest.DiscountRate` |

### Asset level discount — [ACC-R-003](../rules/ACC-R-003.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**if populated, it supersedes ACC-R-001/002 for that asset.**

|  |  |
|---|---|
| When it fires | schedule generation for an equipment lease |
| What it reads | `Asset.DiscountRateOverride` (`sTYPE_PERCENTAGE`) |
| The test | if populated, it supersedes ACC-R-001/002 for that asset |
| What it writes | the rate used in the asset's schedule |

### Discount rate — [ACC-R-004](../rules/ACC-R-004.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**a blank `CodeAccountingMethodID` matches both Finance and Operating. `MinSchedMons`/`MaxSchedMons` bound the schedule length in months for which the rate applies.**

|  |  |
|---|---|
| When it fires | evaluating candidate `DiscountRate` rows |
| What it reads | `DiscountRate.CodeAccountingMethodID`, `.CodeContractUseID`, `.CountryID`/`.CountryIDList`/`.StateProvinceIDList`, `.MinSchedMons`, `.MaxSchedMons`, `.EffectiveThroughDate` |
| The test | a blank `CodeAccountingMethodID` matches both Finance and Operating. `MinSchedMons`/`MaxSchedMons` bound the schedule length in months for which the rate applies |
| What it writes | the matching rate |

### Test 1 title — [ACC-R-005](../rules/ACC-R-005.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`DoesTitleRevertToTenant = true` ⇒ Fail; false ⇒ Pass.**

|  |  |
|---|---|
| When it fires | classification test run |
| What it reads | `ContractFinancialTest.DoesTitleRevertToTenant` (`sTYPE_CHECKBOX`) |
| The test | `DoesTitleRevertToTenant = true` ⇒ Fail; false ⇒ Pass |
| What it writes | `Test1Result` (`sTYPE_PASS_FAIL`) |

### Test 2 purchase — [ACC-R-006](../rules/ACC-R-006.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**true ⇒ Fail; false ⇒ Pass.**

|  |  |
|---|---|
| When it fires | classification test run |
| What it reads | `ContractFinancialTest.ContainsBargainPurchaseOption` |
| The test | true ⇒ Fail; false ⇒ Pass |
| What it writes | `Test2Result` |

### Test 3 major part of — [ACC-R-007](../rules/ACC-R-007.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`< RemainingEconomicLifeThreshold` ⇒ Pass; otherwise Fail. Default threshold "usually set to 75%".**

|  |  |
|---|---|
| When it fires | classification test run |
| What it reads | `TestTermLength`, `RemainingLife`, `CodeRemainingLifeFreqUnitID`, `RemainingEconomicLifeThreshold` |
| The test | `< RemainingEconomicLifeThreshold` ⇒ Pass; otherwise Fail. Default threshold "usually set to 75%" |
| What it computes | `ComputedTestTermLengthToRemainingLife = TestTermLength / RemainingLife` (both normalised to `CodeRemainingLifeFreqUnitID`) |
| What it writes | `ComputedTestTermLengthToRemainingLife` (`sTYPE_PERCENTAGE`), `Test3Result`. - Threshold source ↑ upgraded: the default comes from `Program.RemainingEconomicLifeThreshold` — "Enter the fraction of the economic life of the underlying asset that amounts to a major part of the scheduled accounting period. This value is usually set to 75%. This field is used in the ASC 842 Test." `Contract.RemainingEconomicLifeThreshold` resolves portfolio-level first, then firm-level (ACC-R-004's pattern) |

### Test 3 override — [ACC-R-008](../rules/ACC-R-008.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`IsLeaseNearEnd = true` ⇒ `Test3Result := Pass`, unconditionally.**

|  |  |
|---|---|
| When it fires | after ACC-R-007 |
| What it reads | `IsLeaseNearEnd` (`sTYPE_CHECKBOX`) |
| The test | `IsLeaseNearEnd = true` ⇒ `Test3Result := Pass`, unconditionally |
| What it writes | `Test3Result` |

### Test 4 substantially — [ACC-R-009](../rules/ACC-R-009.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`InitialLiabilityBalance > ThresholdFairValueControlled` ⇒ Fail. Default threshold "usually set to 90%".**

|  |  |
|---|---|
| When it fires | classification test run |
| What it reads | `FairValueOfAsset`, `PortionOfAssetControlled`, `FairValueThreshold`, an initial liability balance |
| The test | `InitialLiabilityBalance > ThresholdFairValueControlled` ⇒ Fail. Default threshold "usually set to 90%" |
| What it computes | ``` FairValueControlled = FairValueOfAsset × PortionOfAssetControlled ThresholdFairValueControlled = FairValueControlled × FairValueThreshold InitLiabilityBalToThreshFairValueCtrld = InitialLiabilityBalance ÷ ThresholdFairValueControlled ``` |
| What it writes | `FairValueControlled`, `ThresholdFairValueControlled`, `InitLiabilityBalToThreshFairValueCtrld`, `Test4Result`. - Threshold source ↑ upgraded: the default comes from `Program.FairValueThreshold` — "Enter the fraction of the fair value of the underlying asset that you would like to test against to determine whether to treat this lease as a financing- / purchase-type lease or an operating lease. This value is usually set to 90%. This field is used in the ASC 842 Test." This is also the first Observed statement that the fair-value test is what decides finance vs. operating |

### Test 5 specialised — [ACC-R-010](../rules/ACC-R-010.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**true ⇒ Fail; false ⇒ Pass.**

|  |  |
|---|---|
| When it fires | classification test run |
| What it reads | `IsAssetTooSpecializedForLessor` |
| The test | true ⇒ Fail; false ⇒ Pass |
| What it writes | `Test5Result` |

### Final classification — [ACC-R-011](../rules/ACC-R-011.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**any result = Fail ⇒ `Finance`; all five = Pass ⇒ `Operating`.**

|  |  |
|---|---|
| When it fires | after ACC-R-005…010 |
| What it reads | `Test1Result` … `Test5Result` |
| The test | any result = Fail ⇒ `Finance`; all five = Pass ⇒ `Operating` |
| What it writes | `ContractFinancialTest.FinalResult` (`sTYPE_TEXT`); `CodeAccountingMethodID` set to the matching Accounting Method code |

### Accounting method — [ACC-R-012](../rules/ACC-R-012.md)

*Derived · rule · source: `docs/modules/accounting/rules.md`*

**if populated, the override supersedes the computed `CodeAccountingMethodID`.**

|  |  |
|---|---|
| When it fires | after ACC-R-011 |
| What it reads | `ContractFinancialTest.CodeAcctMethodOverrideID` ("Accounting Type Override"); `Asset.CodeAccountingMethodOverrideID` |
| The test | if populated, the override supersedes the computed `CodeAccountingMethodID` |
| What it writes | the accounting method used by the schedule |

### Test locking — [ACC-R-013](../rules/ACC-R-013.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**a locked test cannot be modified and cannot be deleted. Superseding is by creating a new `ContractFinancialTest` row.**

|  |  |
|---|---|
| When it fires | user sets `IsLocked = true` |
| The test | a locked test cannot be modified and cannot be deleted. Superseding is by creating a new `ContractFinancialTest` row |
| What it writes | immutable test record |

### Propagation of the — [ACC-R-014](../rules/ACC-R-014.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**take the most recently locked row's `FinalResult`.**

|  |  |
|---|---|
| When it fires | a `ContractFinancialTest` is locked |
| What it reads | all `ContractFinancialTest` rows for the contract with `IsLocked = true` |
| What it computes | take the most recently locked row's `FinalResult` |
| What it writes | `Contract.LatestFinancialTestFinalResult` |

### Auto computation — [ACC-R-015](../rules/ACC-R-015.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`ContractFinancialTest.AutoComputed := true`.**

|  |  |
|---|---|
| When it fires | the system runs the test without user entry |
| What it writes | `ContractFinancialTest.AutoComputed := true` |

### Term length sourcing — [ACC-R-016](../rules/ACC-R-016.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**- `TermLength = ExpireDate − CommenceDate` (the contractual term); - `LikelyTermLength` = length through the last term marked Likely on `Abstract Info > Terms`; - `LastLikelyOptionDate` = end date of the last likely term; - `TestTermLength` = length of the term selected for the test.**

|  |  |
|---|---|
| When it fires | classification test run |
| What it reads | `Contract.CommenceDate`, `Contract.ExpireDate`, `ContractTerm` rows marked Likely |
| What it computes | - `TermLength = ExpireDate − CommenceDate` (the contractual term); - `LikelyTermLength` = length through the last term marked Likely on `Abstract Info > Terms`; - `LastLikelyOptionDate` = end date of the last likely term; - `TestTermLength` = length of the term selected for the test |
| What it writes | the three term-length fields |

### Accounting date — [ACC-R-017](../rules/ACC-R-017.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`Topic842BeginDate = max(adoption date, PossessionBeginDate)`; `Topic842EndDate` = end of accounting including likely options.**

|  |  |
|---|---|
| When it fires | classification test creation |
| What it reads | firm's ASC 842 adoption date; `Contract.PossessionBeginDate`; likely options |
| What it computes | `Topic842BeginDate = max(adoption date, PossessionBeginDate)`; `Topic842EndDate` = end of accounting including likely options |
| What it writes | `Topic842BeginDate`, `Topic842EndDate` |

### Asset level — [ACC-R-018](../rules/ACC-R-018.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**if the override dates are populated they set the test/schedule window; otherwise the window comes from the expense schedule dates.**

|  |  |
|---|---|
| When it fires | test or schedule run for an `Asset` |
| What it reads | `Asset.AccountingBeginDate`, `Asset.AccountingEndDate`, the asset's expense schedules |
| The test | if the override dates are populated they set the test/schedule window; otherwise the window comes from the expense schedule dates |

### Covenant sourced — [ACC-R-019](../rules/ACC-R-019.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**the covenant amount is pulled into the accounting assumptions and the accounting schedule only if `CodeAccountingAdjustmentTypeID` ∈ {`Purchase Option`, `Cancellation Option`, `Residual Value Guarantee`}.**

|  |  |
|---|---|
| When it fires | classification test or schedule creation |
| What it reads | `Covenant.CovenantAmount`, `Covenant.CodeAccountingAdjustmentTypeID`, `Covenant.TotalRVGAmount`, `Covenant.ThirdPartyRVGAmount` |
| The test | the covenant amount is pulled into the accounting assumptions and the accounting schedule only if `CodeAccountingAdjustmentTypeID` ∈ {`Purchase Option`, `Cancellation Option`, `Residual Value Guarantee`} |
| What it writes | `ContractFinancialTest.PurchaseOptionAmount` / `.CancellationOptionAmount` / `.ResidualValueGuarantees`, and the same three fields on `SLSummary` |

### Schedule type change — [ACC-R-020](../rules/ACC-R-020.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`SLSummary.NeedsRecalculation := true`.**

|  |  |
|---|---|
| When it fires | `CodeASC842ScheduleID` or `CodeIFRS16ScheduleID` is changed on the Accounting Assumptions page, the Covenants page, or the Recurring Expenses page |
| What it writes | `SLSummary.NeedsRecalculation := true` |

### New expense setup — [ACC-R-021](../rules/ACC-R-021.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**the accounting schedule associated with that expense type is dirtied.**

|  |  |
|---|---|
| When it fires | an `ExpenseSetup` with an `ExpenseSchedule` is created |
| What it reads | `ExpenseSetup.CodeExpenseTypeID`; `SLSummary.AssociatedExpenseSetupIDs` |
| The test | the accounting schedule associated with that expense type is dirtied |
| What it writes | `SLSummary.NeedsRecalculation := true`; `AssociatedExpenseSetupIDs` extended |

### Accounting method — [ACC-R-022](../rules/ACC-R-022.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**the schedule must be remeasured.**

|  |  |
|---|---|
| When it fires | `SLSummary.CodeAccountingMethodID` is changed |
| What it writes | the schedule must be remeasured |

### Recalculation audit — [ACC-R-023](../rules/ACC-R-023.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`RecalcTriggerDate := today`; `NeedsRecalcModifiedByLastMember := current member`; current member appended to `NeedsRecalcModifiedByMemberIDList`.**

|  |  |
|---|---|
| When it fires | `NeedsRecalculation` transitions to true |
| What it writes | `RecalcTriggerDate := today`; `NeedsRecalcModifiedByLastMember := current member`; current member appended to `NeedsRecalcModifiedByMemberIDList` |

### Recalculation — [ACC-R-024](../rules/ACC-R-024.md)

*Derived · rule · source: `docs/modules/accounting/rules.md`*

**a `RecalcOverrideNotes` row linked by `SLSummaryID`; the id appended to `SLSummary.RecalcOverrideNotesIDList`.**

|  |  |
|---|---|
| When it fires | a user declines to recalculate a dirty schedule |
| What it reads | free text |
| What it writes | a `RecalcOverrideNotes` row linked by `SLSummaryID`; the id appended to `SLSummary.RecalcOverrideNotesIDList` |

### Cash flow routing by — [ACC-R-025](../rules/ACC-R-025.md)

*Derived · rule · source: `docs/modules/accounting/rules.md`*

**each period cash flow is routed to the schedule type its expense type designates for the standard being generated.**

|  |  |
|---|---|
| When it fires | schedule generation |
| What it reads | `ExpenseSchedule` rows → `ExpenseSetup.CodeExpenseTypeID` → `CodeExpenseType.CodeSLScheduleID` / `.CodeASC842ScheduleID` / `.CodeIFRS16ScheduleID` |
| What it computes | each period cash flow is routed to the schedule type its expense type designates for the standard being generated |
| What it writes | `SLPeriod.PeriodCashAmount` populated on the correct `SLSummary` |

### Secondary schedule — [ACC-R-026](../rules/ACC-R-026.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**the stated percentage of the amount is allocated to a secondary schedule; the remainder to the primary.**

|  |  |
|---|---|
| When it fires | an `AcctingAssumptionAdjust` or `Covenant` carries a secondary allocation |
| What it reads | `AcctingAssumptionAdjust.SecondaryRentSchedAllocPercent`, `Covenant.SecondaryRentSchedAllocPercent` |
| What it computes | the stated percentage of the amount is allocated to a secondary schedule; the remainder to the primary |

### Schedule type flag — [ACC-R-027](../rules/ACC-R-027.md)

*Derived · rule · source: `docs/modules/accounting/rules.md`*

**`SLSummary.IsSLSchedule` / `.IsASC842Schedule` / `.IsIFRS16Schedule := true` respectively.**

|  |  |
|---|---|
| When it fires | `GenerateStraightLineRent` / `GenerateFASBSchedule` / `GenerateIFRS16Schedule` |
| What it writes | `SLSummary.IsSLSchedule` / `.IsASC842Schedule` / `.IsIFRS16Schedule := true` respectively |
| Live state | only `IsASC842Schedule` is reachable today. With the straight-line and IFRS 16 schedule-type tables empty, `GenerateStraightLineRent` and `GenerateIFRS16Schedule` have no schedule type to stamp. Any existing row with `IsSLSchedule` or `IsIFRS16Schedule` true is an orphan whose schedule type was deleted — worth querying as a data-integrity check. Observed for the empty tables; Derived for the consequence |

### Period row — [ACC-R-028](../rules/ACC-R-028.md)

*Derived · rule · source: `docs/modules/accounting/rules.md`*

**one `SLPeriod` per fiscal period, carrying `BeginDate`, `EndDate`, `NumberDays`, `FiscalPeriod`, `FiscalPeriodYear`, `CumulativePeriodNumber`.**

|  |  |
|---|---|
| When it fires | schedule generation |
| What it reads | `FiscalPeriod` rows for the contract's `ProgramID` covering `SLSummary.BeginDate` … `EndDate` |
| What it computes | one `SLPeriod` per fiscal period, carrying `BeginDate`, `EndDate`, `NumberDays`, `FiscalPeriod`, `FiscalPeriodYear`, `CumulativePeriodNumber` |
| What it writes | N `SLPeriod` rows; `SLSummary.SLTermLength` = "The number of periods in the straight line schedule term." |

### GL account — [ACC-R-029](../rules/ACC-R-029.md)

*Derived · rule · source: `docs/modules/accounting/rules.md`*

**copied verbatim onto `SLPeriod.ExportAcct1Number` … `ExportAcct20Number`.**

|  |  |
|---|---|
| When it fires | `SLPeriod` row creation |
| What it reads | the governing schedule type's `ExportAcct1Number` … `ExportAcct20Number` |
| What it writes | copied verbatim onto `SLPeriod.ExportAcct1Number` … `ExportAcct20Number` |

### Suppress ROU asset — [ACC-R-030](../rules/ACC-R-030.md)

*Inferred · rule · source: `docs/modules/accounting/rules.md`*

**if true, no asset amortization is recognised for schedules of this type.**

|  |  |
|---|---|
| When it fires | schedule generation |
| What it reads | the schedule type's `DontAmortizeAssetValue` (Boolean) |
| The test | if true, no asset amortization is recognised for schedules of this type |
| What it writes | `SLPeriod.PeriodAssetAmortizationExpense` suppressed |

### Initial liability — [ACC-R-031](../rules/ACC-R-031.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`InitialLiabilityBalance = Σ PVOfPeriodCashAmount`.**

|  |  |
|---|---|
| When it fires | schedule generation |
| What it reads | `SLPeriod.PVOfPeriodCashAmount` for every period |
| What it computes | `InitialLiabilityBalance = Σ PVOfPeriodCashAmount` |
| What it writes | `SLSummary.InitialLiabilityBalance`, `SLPeriod.InitialLiabilityBalance` |

### Present value of a — [ACC-R-032](../rules/ACC-R-032.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**discount the cash payment from the period it is made back to the accounting begin date.**

|  |  |
|---|---|
| When it fires | period row generation |
| What it reads | `SLPeriod.PeriodCashAmount`, the discount rate (ACC-R-001…003), the period offset |
| What it computes | discount the cash payment from the period it is made back to the accounting begin date |
| What it writes | `SLPeriod.PVOfPeriodCashAmount` |

### Initial asset — [ACC-R-033](../rules/ACC-R-033.md)

*Inferred · rule · source: `docs/modules/accounting/rules.md`*

**not documented. The vendor says only "This is a calculated value which contains the initial value over the asset of the lease.".**

|  |  |
|---|---|
| When it fires | schedule generation |
| What it reads | `InitialLiabilityBalance`, `InitialDirectCostAmount`, `LeaseIncentiveAmount`, `PreCommencePayAmount`, `DismantlingStorageCostAmount`, `InitialAssetBalanceAdjust` |
| What it computes | not documented. The vendor says only "This is a calculated value which contains the initial value over the asset of the lease." |
| What it writes | `SLSummary.InitialAssetBalance` |

### Balance forward and — [ACC-R-034](../rules/ACC-R-034.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**``` BalanceForward = InitialAssetBalance − InitialLiabilityBalance BalanceForward = RemeasurementBalanceForward + ProfitAndLossImpact ```.**

|  |  |
|---|---|
| When it fires | schedule creation that carries balances from a prior schedule |
| What it computes | ``` BalanceForward = InitialAssetBalance − InitialLiabilityBalance BalanceForward = RemeasurementBalanceForward + ProfitAndLossImpact ``` |
| What it writes | `BalanceForward`, `RemeasurementBalanceForward` (label "Balance Sheet Impact"), `ProfitAndLossImpact` |

### Total commitment — [ACC-R-035](../rules/ACC-R-035.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`TotalCommitment = Σ` all rent payments for the life of the lease.**

|  |  |
|---|---|
| What it computes | `TotalCommitment = Σ` all rent payments for the life of the lease |
| What it writes | `SLSummary.TotalCommitment` |

### Remaining cash — [ACC-R-036](../rules/ACC-R-036.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`CurrentRemainingCashBalance` = sum of current and future remaining lease payments; `CurrentRemainingCashBalanceAfterReportEnd` = the same excluding the current period.**

|  |  |
|---|---|
| What it computes | `CurrentRemainingCashBalance` = sum of current and future remaining lease payments; `CurrentRemainingCashBalanceAfterReportEnd` = the same excluding the current period |

### Straight line — [ACC-R-037](../rules/ACC-R-037.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**``` PeriodExpenseAmount[n] = total cash rent straight-lined over the schedule life PeriodDeferredAmount[n] = PeriodCashAmount[n] − PeriodExpenseAmount[n] CumulativeDeferredBalance[n] = Σ(1..n) PeriodDeferredAmount ```.**

|  |  |
|---|---|
| When it fires | period row generation |
| What it computes | ``` PeriodExpenseAmount[n] = total cash rent straight-lined over the schedule life PeriodDeferredAmount[n] = PeriodCashAmount[n] − PeriodExpenseAmount[n] CumulativeDeferredBalance[n] = Σ(1..n) PeriodDeferredAmount ``` |

### Interest expense — [ACC-R-038](../rules/ACC-R-038.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`PeriodInterestAmount[n]` = interest owed on the liability, at the discount rate.**

|  |  |
|---|---|
| What it computes | `PeriodInterestAmount[n]` = interest owed on the liability, at the discount rate |

### Accumulated — [ACC-R-039](../rules/ACC-R-039.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**``` CumulativeAssetAmortExpense[1] = PeriodAssetAmortizationExpense[1] CumulativeAssetAmortExpense[n] = PeriodAssetAmortizationExpense[n] + CumulativeAssetAmortExpense[n−1] ```.**

|  |  |
|---|---|
| What it computes | ``` CumulativeAssetAmortExpense[1] = PeriodAssetAmortizationExpense[1] CumulativeAssetAmortExpense[n] = PeriodAssetAmortizationExpense[n] + CumulativeAssetAmortExpense[n−1] ``` |

### Gross asset balance — [ACC-R-040](../rules/ACC-R-040.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`GrossAssetBalance[n] = AssetAmount[n] + CumulativeAssetAmortExpense[n]`.**

|  |  |
|---|---|
| What it computes | `GrossAssetBalance[n] = AssetAmount[n] + CumulativeAssetAmortExpense[n]` |

### Short term long term — [ACC-R-041](../rules/ACC-R-041.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**``` Forward12MonthAssetChange[n] = Σ asset amortization, periods n+1 … n+12 Forward12MonthLiabilityChange[n] = Σ liability amortization, periods n+1 … n+12 ShortTermRentExpense[n] = Σ rent expense, next 12 months LongTermRentExpense[n] = Σ rent expense beyond 12 months LongTermLiability[n] =….**

|  |  |
|---|---|
| What it computes | ``` Forward12MonthAssetChange[n] = Σ asset amortization, periods n+1 … n+12 Forward12MonthLiabilityChange[n] = Σ liability amortization, periods n+1 … n+12 ShortTermRentExpense[n] = Σ rent expense, next 12 months LongTermRentExpense[n] = Σ rent expense beyond 12 months LongTermLiability[n] = LiabilityAmount[n] − shortTermLiability[n] ``` |

### Which short term — [ACC-R-042](../rules/ACC-R-042.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`Liability Amortization Based` ⇒ use `Forward12MonthLiabilityAmortBased`; `PV Based` ⇒ use `Forward12MonthLiabilityPVBased`.**

|  |  |
|---|---|
| When it fires | reporting the short-term liability |
| What it reads | firm setting Short-Term/Long-Term Liability Calculation Method (`Admin > Manage Company > Financial Settings`) |
| The test | `Liability Amortization Based` ⇒ use `Forward12MonthLiabilityAmortBased`; `PV Based` ⇒ use `Forward12MonthLiabilityPVBased` |

### Currency translation — [ACC-R-043](../rules/ACC-R-043.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**``` AssetTranslationAdjustment = AssetBalance[n] + AssetAmortizationExpense[n] − AssetBalance[n−1] LiabilityTranslationAdjustment = AssetBalance[n] + (Payment[n] − Interest[n]) − AssetBalance[n−1] CumulativeTranslationAdjustment = AssetTranslationAdjustment[n] − LiabilityTranslationAdjustment[n]….**

|  |  |
|---|---|
| When it fires | FX impact calculated on the Rent Schedule page for a contract with `Contract.IsTranslation = true` |
| What it computes | ``` AssetTranslationAdjustment = AssetBalance[n] + AssetAmortizationExpense[n] − AssetBalance[n−1] LiabilityTranslationAdjustment = AssetBalance[n] + (Payment[n] − Interest[n]) − AssetBalance[n−1] CumulativeTranslationAdjustment = AssetTranslationAdjustment[n] − LiabilityTranslationAdjustment[n] LiabilityFXImpact = (FXrate[n] − FXrate[initial]) × LiabilityBalance[n] ``` |

### Translation vs — [ACC-R-044](../rules/ACC-R-044.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`Contract.IsTranslation = true` ⇒ use the Translation mapping on `Admin > Manage Company > Financial Settings`; false ⇒ the Revaluation mapping.**

|  |  |
|---|---|
| The test | `Contract.IsTranslation = true` ⇒ use the Translation mapping on `Admin > Manage Company > Financial Settings`; false ⇒ the Revaluation mapping |

### Schedule — [ACC-R-045](../rules/ACC-R-045.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**``` new.PriorLastPostedPeriodID := old.SLSummaryID new.PostedEndDate := old.LastPostedEndDate new.PostedInitAssetAdj := new.InitialAssetBalance − old.AssetBalance(as of PostedEndDate) new.PostedInitLiabilityAdj := new.InitialLiabilityBalance − old.LiabilityBalance(as of PostedEndDate)….**

|  |  |
|---|---|
| When it fires | a replacement `SLSummary` is created for a contract |
| What it writes | ``` new.PriorLastPostedPeriodID := old.SLSummaryID new.PostedEndDate := old.LastPostedEndDate new.PostedInitAssetAdj := new.InitialAssetBalance − old.AssetBalance(as of PostedEndDate) new.PostedInitLiabilityAdj := new.InitialLiabilityBalance − old.LiabilityBalance(as of PostedEndDate) new.PostedAssetAdjustment := new.AssetBalance − old.lastPostedAssetBalance new.PostedLiabilityAdjustment := new.LiabilityBalance − old.lastPostedLiabilityBalance old.Inactive := true old.InactiveDate := today ``` |

### Mid period — [ACC-R-046](../rules/ACC-R-046.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`InterestBeforeMidRemeasure` = partial interest from period start to the remeasurement date; `SleBeforeMidRemeasure` = partial single lease expense over the same span.**

|  |  |
|---|---|
| When it fires | remeasurement effective mid-period |
| What it writes | `InterestBeforeMidRemeasure` = partial interest from period start to the remeasurement date; `SleBeforeMidRemeasure` = partial single lease expense over the same span |

### Term shortening — [ACC-R-047](../rules/ACC-R-047.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**``` ShortenedSchedInitLiabilityBal = initial liability over the remaining periods of the original schedule, restated over the shortened term ShortenedLeaseLiabilityDiff = ShortenedSchedInitLiabilityBal − original.lastPostedPeriod.LiabilityBalance ShortenedTermAssetDiff = −1 × Σ….**

|  |  |
|---|---|
| When it fires | early termination / term shortening |
| What it computes | ``` ShortenedSchedInitLiabilityBal = initial liability over the remaining periods of the original schedule, restated over the shortened term ShortenedLeaseLiabilityDiff = ShortenedSchedInitLiabilityBal − original.lastPostedPeriod.LiabilityBalance ShortenedTermAssetDiff = −1 × Σ AssetAmortizationExpense over the remaining periods of the original schedule within the shortened term ShortenedTermRentDiff = new.InitialLiabilityBalance − ShortenedSchedInitLiabilityBal ``` |

### Impairment — [ACC-R-048](../rules/ACC-R-048.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`TotalImpairmentImpact = ImpairmentAmount + PriorAccumulatedAmortizationBalance`, where `PriorAccumulatedAmortizationBalance` is "the Accumulated Amortization Balance of the accounting period prior to the impairment.".**

|  |  |
|---|---|
| When it fires | an impairment amount is entered |
| What it reads | `SLSummary.ImpairmentAmount` / `ContractFinancialTest.ImpairmentsAmount` / `Asset.ImpairmentOverride` — all three entered as negative numbers |
| What it computes | `TotalImpairmentImpact = ImpairmentAmount + PriorAccumulatedAmortizationBalance`, where `PriorAccumulatedAmortizationBalance` is "the Accumulated Amortization Balance of the accounting period prior to the impairment." |

### Final asset amount — [ACC-R-049](../rules/ACC-R-049.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**these four fields are "only made available on Finance contracts". `FinalAssetDate` defaults to the schedule end date; a later date amortizes beyond the schedule end.**

|  |  |
|---|---|
| When it fires | schedule generation on a Finance contract |
| What it reads | `SLSummary.FinalAssetAmount`, `.FinalAssetAllocPercent`, `.FinalAssetDate`, `.SLRemainingAssetBalance` |
| The test | these four fields are "only made available on Finance contracts". `FinalAssetDate` defaults to the schedule end date; a later date amortizes beyond the schedule end |
| What it writes | the asset balance remaining after the schedule end date |

### Approval is — [ACC-R-050](../rules/ACC-R-050.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`SLSummary.IsApproved := true`, and cannot be reversed through the application.**

|  |  |
|---|---|
| When it fires | the ASC 842 Schedule Review/Approval workflow reaches its third step and the client approver accepts (see `ACC-R-060`) |
| The test | `SLSummary.IsApproved := true`, and cannot be reversed through the application |

### Posting state and — [ACC-R-051](../rules/ACC-R-051.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**`SLSummary.LastPostedDate` = begin date of the last posted period; `LastPostedEndDate` = its end date; `LastBalancePosted` = "the asset minus the liability as of the last posted period"; `LastPostedBalanceSheetImpact` = "the portion of the last posted balance that is to remain on the balance sheet.".**

|  |  |
|---|---|
| What it reads | `SLPeriod.RecordStatus` ∈ {posted, not posted, mixed}; `SLPeriod.PostedDate` |
| What it writes | `SLSummary.LastPostedDate` = begin date of the last posted period; `LastPostedEndDate` = its end date; `LastBalancePosted` = "the asset minus the liability as of the last posted period"; `LastPostedBalanceSheetImpact` = "the portion of the last posted balance that is to remain on the balance sheet." |

### Accrual amount — [ACC-R-052](../rules/ACC-R-052.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**entering any one of annual amount / period amount / accrual rate auto-populates the other two plus `FirstPaymentAmount` and `LastPaymentAmount`. Rate-based entry requires `RentableArea` to be populated — "If you are not going to use rentable area, do not enter 0. Leave this field blank.".**

|  |  |
|---|---|
| When it fires | `ExpenseAccrualSchedule` entry |
| What it reads | `AnnualAmount` or `PeriodAmount` or `AccrualRate`; `ExpenseAccrualSetup.RentableArea`; `DailyAccrualRate` when `ExpenseAccrualSetup.IsDailyRent = true` |
| What it computes | entering any one of annual amount / period amount / accrual rate auto-populates the other two plus `FirstPaymentAmount` and `LastPaymentAmount`. Rate-based entry requires `RentableArea` to be populated — "If you are not going to use rentable area, do not enter 0. Leave this field blank." |
| What it writes | `AnnualAmount`, `PeriodAmount`, `AccrualRate`, `FirstPaymentAmount`, `LastPaymentAmount` |

### Accrual transaction — [ACC-R-053](../rules/ACC-R-053.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**- `TaxesIncludedFlag = false` ⇒ `TotalAmount = PeriodAmount + Σ TaxAmountN`, `TotalAmount` read-only. - `TaxesIncludedFlag = true` ⇒ `PeriodAmount` becomes disabled, the system subtracts the tax amounts from the period amount, and `TotalAmount` becomes editable.**

|  |  |
|---|---|
| When it fires | `AccrualTransaction` entry |
| What it reads | `PeriodAmount`, `TaxAmount1` … `TaxAmount4`, `TaxesIncludedFlag` |
| The test | - `TaxesIncludedFlag = false` ⇒ `TotalAmount = PeriodAmount + Σ TaxAmountN`, `TotalAmount` read-only. - `TaxesIncludedFlag = true` ⇒ `PeriodAmount` becomes disabled, the system subtracts the tax amounts from the period amount, and `TotalAmount` becomes editable |

### Accrual transaction — [ACC-R-054](../rules/ACC-R-054.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**"Warning - once you mark a transaction as processed, you cannot change it.".**

|  |  |
|---|---|
| When it fires | `AccrualTransaction.ProcessedFlag := true` |
| The test | "Warning - once you mark a transaction as processed, you cannot change it." |

### Alternate rent hold — [ACC-R-055](../rules/ACC-R-055.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**a hold flag is set on all recurring-expense transactions (resp. percentage-rent transactions) generated during the window.**

|  |  |
|---|---|
| When it fires | rent generation while the contract is in an alternate-rent window |
| What it reads | `AlternateRentSchedule.SetExpHoldFlag`, `.SetPRHoldFlag`, `.BeginDate`, `.EndDate` |
| What it writes | a hold flag is set on all recurring-expense transactions (resp. percentage-rent transactions) generated during the window |

### Amortisation basis — [ACC-R-056](../rules/ACC-R-056.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**- `PER_PERIOD` ⇒ "distributes the amortization equally among periods"; - `PER_DAY` ⇒ "distributes the amortization according to the number of days in the period", i.e. weight each period by `SLPeriod.NumberDays`.**

|  |  |
|---|---|
| When it fires | schedule generation |
| What it reads | `Program.SLAssetAmortizeMethod`, `Program.SLCashAmortizeMethod`, `Program.SLExpenseAmortizeMethod` — three independent `sTYPE_TEXT` switches, one per schedule column |
| The test | - `PER_PERIOD` ⇒ "distributes the amortization equally among periods"; - `PER_DAY` ⇒ "distributes the amortization according to the number of days in the period", i.e. weight each period by `SLPeriod.NumberDays` |
| What it writes | `SLPeriod.PeriodAssetAmortizationExpense` (asset switch), `SLPeriod.PeriodCashAmount` (cash switch), `SLPeriod.PeriodExpenseAmount` (expense switch) |
| Scope restriction | ⚠ "This setting impacts only ASC 842 Finance leases." — stated on the asset switch. The cash and expense switches carry no such restriction |
| Rebuild note | three separate switches, not one. A single "amortisation basis" setting in ASG Edge+ would be a behavio |

### 28 day proration of — [ACC-R-057](../rules/ACC-R-057.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**when true, prorate the partial period on a 28-day multiplier; and "If you have a partial period that is greater than or equal to 28 days, it will be considered a whole period by the system and will not prorate.".**

|  |  |
|---|---|
| When it fires | schedule generation where the first or last period is partial |
| What it reads | `Program.SLProrate35As28` (`Boolean`); the partial period's day count |
| The test | when true, prorate the partial period on a 28-day multiplier; and "If you have a partial period that is greater than or equal to 28 days, it will be considered a whole period by the system and will not prorate." |
| Scope restriction | "This setting only impacts values calculated per period" — i.e. it interacts with `ACC-R-056`: it is inert on columns set to `PER_DAY` |

### Fiscal calendar year — [ACC-R-058](../rules/ACC-R-058.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**"This field determines if the program allows for matching of fiscal/calendar year rent.".**

|  |  |
|---|---|
| When it fires | schedule generation and the fiscal-year rollups |
| What it reads | `Program.SLMatchYearEnds` (`Boolean`), `Program.FiscalYearEnd` (`Date` — month and day) |
| The test | "This field determines if the program allows for matching of fiscal/calendar year rent." |

### Per column FX rate — [ACC-R-059](../rules/ACC-R-059.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**the `Sub` variant applies to "contracts in need of translation", the plain variant to "contracts in need of revaluation" — selected per contract by `Contract.IsTranslation` (`ACC-R-044`).**

|  |  |
|---|---|
| When it fires | currency conversion of a schedule column |
| What it reads | fourteen `Program` code fields — seven schedule columns × two modes: |
| The test | the `Sub` variant applies to "contracts in need of translation", the plain variant to "contracts in need of revaluation" — selected per contract by `Contract.IsTranslation` (`ACC-R-044`) |
| What it writes | the exchange rate applied to each `SLPeriod` `*Translated` column |
| Rebuild note | this partly answers the `ACC-R-044` open question. The Translation-vs-Revaluation mapping referenced by `Contract.IsTranslation` is portfolio-scoped and column-by-column, not a single firm-wide toggle. The `Exchange Rate Type Code` values themselves are still unknown. --- ## J. The ASC 842 approval workflow *Added 2026-09-10 from the live tenant. The full capture is in `docs/modules/layouts-and-forms/forms-vs-pages-vs-layouts.md`.* |

### Schedules are not — [ACC-R-060](../rules/ACC-R-060.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**on completion of step 3, `SLSummary.IsApproved := true` (`ACC-R-050`).**

|  |  |
|---|---|
| When it fires | an ASC 842 schedule is generated and submitted for review |
| What it writes | on completion of step 3, `SLSummary.IsApproved := true` (`ACC-R-050`) |
| Process | the tenant runs a live workflow named "ASC 842 Schedule Review/Approval", three ordered steps, every step `Type = Form`, every step `Approval Level = Member`: |
| Rebuild note | ⚠ This is the single most consequential live finding for this module. The accounting engine's output is not published by the calculation. It is generated, reviewed once, approved by ASG, then approved by the client — a two-party sign-off with an internal review ahead of it. A schedule therefore has a state, not a Boolean: `draft → submitted → under review → ASG-approved → client-approved`. Nothing in the offline schema sh |

### What an ASC 842 — [ACC-R-061](../rules/ACC-R-061.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**the form type declares attachability as a Boolean per entity kind. For this form type, only `Portfolio` = Yes and `RE Contract` = Yes; `Capital Program`, `Prototype`, `Location`, `Parcel`, `Site`, `Project`, `Facility`, `Capital Project` and `Equipment Contract` are all `No`.**

|  |  |
|---|---|
| When it fires | creating an `ASC 842 Schedule Review/Approval` request |
| The test | the form type declares attachability as a Boolean per entity kind. For this form type, only `Portfolio` = Yes and `RE Contract` = Yes; `Capital Program`, `Prototype`, `Location`, `Parcel`, `Site`, `Project`, `Facility`, `Capital Project` and `Equipment Contract` are all `No` |
| Rebuild note | ⚠ `Equipment Contract` is `No`. Equipment leases generate ASC 842 schedules (`Asset` carries the full classification field set, and `Contract.GenerateFASBSchedule` exists on equipment contracts) but cannot be routed through this review workflow. Either equipment schedules bypass the approval gate entirely, or they are reviewed at the portfolio level. This is a real process gap and is now the top open question below |

### Approver resolution — [ACC-R-062](../rules/ACC-R-062.md)

*Observed · rule · source: `docs/modules/accounting/rules.md`*

**all three ASC 842 steps use `Member` — a named approver, resolved at configuration time rather than by org-chart position.**

|  |  |
|---|---|
| When it fires | a workflow step activates |
| What it reads | the step's `Approval Level` ∈ {`Member`, `Job Title`, `Ad Hoc`} |
| The test | all three ASC 842 steps use `Member` — a named approver, resolved at configuration time rather than by org-chart position |
